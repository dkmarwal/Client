pipeline {
    agent none
    stages {
		stage('Sonar Code Quality') {
            when {
                branch 'client_usbank_aws'
            }
            agent {label 'master'}
			environment {
				scannerHome = tool 'sonar-cli'
			}
            steps {
                script {
					withSonarQubeEnv('sonar') {
						sh "${scannerHome}/bin/sonar-scanner -Dsonar.projectKey=client-portal"
					}
				}
            }
        }
        stage('Install NodeJS dependencies') {
            when {
                branch 'client_usbank_aws'
            }
            agent {label 'master'}
            steps {
                dir('./SourceCode/Frontend/client_portal') {
					sh 'npm install'
                }
				dir('./SourceCode/Frontend/client_portal/server') {
					sh 'npm install'
                }
            }
        }
        stage('Build NodeJS') {
            when {
                branch 'client_usbank_aws'
            }
            agent {label 'master'}
            steps {
                dir('./SourceCode/Frontend/client_portal') {
					sh 'REACT_APP_STAGE=USBANK_DEV PUBLIC_URL=/ yarn build'
					sh 'cp -r build server/'
                }
            }
        }
        stage('Deploy_UAT') {
            when {
                branch 'client_usbank_aws'
            }
            agent {label 'master'}
            steps {
                input 'Deploy to AWS?'
                milestone(1)
                sshPublisher(publishers: [sshPublisherDesc(configName: 'usbank-webserver', transfers: [sshTransfer(cleanRemote: false, excludes: '', execCommand: '''pwd; chmod +x /client_portal/deploy_run.sh; sh /client_portal/deploy_run.sh;''', execTimeout: 30000, flatten: false, makeEmptyDirs: false, noDefaultExcludes: false, patternSeparator: '[, ]+', remoteDirectory: 'client_portal', remoteDirectorySDF: false, removePrefix: 'SourceCode/Frontend/client_portal/server', sourceFiles: 'SourceCode/Frontend/client_portal/server/**/*')], usePromotionTimestamp: false, useWorkspaceInPromotion: false, verbose: true)])			
            }
            post {
		       always {
                    cleanWs()
               }
            }
        }
    }
}
